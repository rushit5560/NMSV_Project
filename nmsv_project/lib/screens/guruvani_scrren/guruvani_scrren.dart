import 'package:flutter/material.dart';

class GuruVaniScreen extends StatelessWidget {
  const GuruVaniScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
