import 'package:flutter/material.dart';

class MeetWithGurudevScreen extends StatelessWidget {
  const MeetWithGurudevScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
